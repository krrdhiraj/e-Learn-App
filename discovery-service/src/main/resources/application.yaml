spring:
  application:
    name: discovery-service

# configuration to not register with server(itself) or to not became client itself
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
